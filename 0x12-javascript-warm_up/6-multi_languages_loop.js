#!/usr/bin/node

/* script that prints 3 lines:
but by using an array of string
and a loop */

const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (const i in array) {
  console.log(array[i]);
}
